# Task: X3 Round 4 — X4 final audit FAIL 항목 4 카테고리 근본수정

X4 final audit verdict = FAIL. X2-1 / X2-2 / X2-3 SDD 의도와 현 상태 불일치를 일괄 시정.

## Pre-reads

- X4 audit 결과: `/tmp/codex-logs/x4-final-audit.log`
- X2-1 SDD: `.specs/verodesign/2026-05-21-slot-group-reorganization/spec.md` (line 30-33 brand 별 implements 의도)
- X2-2 SDD: `.specs/verodesign/2026-05-21-dtcg-naming-migration/spec.md` (naming rename 전수 적용)
- X2-3 SDD: `.specs/verodesign/2026-05-21-contrast-policy-v2/spec.md` (aaa-strict optional 정책)
- 토큰 SoT 정독: `packages/design/tokens/{semantic,themes}/*.json`, `packages/design/dist/css/themes/*.css`, `packages/design/dist/data/contrast-report.json`

## 작업 (4 카테고리)

### Cat 1. brand `implements` SDD 정합 (X2-1 line 33)

**SDD 의도** (X2-1 spec line 30-33):
- `default-dark`, `veronex-dark`, `girok-{light,dark}` → `["core", "web", "status"]` (현 binding 보존)
- 그 외 (`verobase-{light,dark}`, `verobase-admin-{light,dark}`, `veronex-light`, `_template-{light,dark}`) → `["core", "web"]` (status 미구현 = 정상)

**현 상태**: 11/11 theme 모두 `["core","web","status"]` (over-binding).

**작업**:
1. 다음 theme 파일의 `$extensions.verobee.implements` 에서 `"status"` 제거:
   - `tokens/themes/verobase-light.json`
   - `tokens/themes/verobase-dark.json`
   - `tokens/themes/verobase-admin-light.json`
   - `tokens/themes/verobase-admin-dark.json`
   - `tokens/themes/veronex-light.json`
   - `tokens/themes/_template-light.json`
   - `tokens/themes/_template-dark.json` (template 도 status 미선언이 기본값)
2. 위 theme 파일의 status slot binding (`theme.status.{success,warning,error,info,neutral}` 및 그 `.foreground`, `theme.destructive`/`theme.destructive.foreground`) 제거. 단, 다음은 보존:
   - `success-bg / error-bg / warning-bg / info-bg / neutral-bg` (semantic/core.json 의 universal background, status group 과 별개)
   - `cancelled`, `cancelled-foreground` (status group 아님)
   - `accent-2`, `accent-2.foreground`, `accent-3`, `accent-3.foreground` (별 slot)
3. 보존되는 4 theme (`default-dark`, `veronex-dark`, `girok-light`, `girok-dark`) 는 무변경.

**Consumer 영향 검토 (REQUIRED 사전)**:
```sh
# 다른 패키지의 verobase / verobase-admin / veronex-light status 사용
rg -- '--vds-theme-status-' packages/design-elements/src packages/design-react/src apps/
rg -- '--vds-theme-destructive(-foreground)?' packages/design-elements/src packages/design-react/src apps/
```

design-elements/design-react 컴포넌트가 status/destructive 변수 사용 시: 컴포넌트는 모든 brand 에서 동작 보장이 contract → status 미bind brand (`verobase-*` 등) 에서 그 컴포넌트는 어떻게 fallback?

→ 권장 fallback: status 미bind brand 의 컴포넌트 사용시 `var(--vds-theme-status-success, var(--vds-theme-primary))` 같은 graceful fallback. **단 본 task scope 밖** — 보고만, 본 task 는 implements/binding 정리만.

만약 fallback 없이 컴포넌트가 깨질 위험이 있으면 다음 중 택1, 보고:
- (A) implements 의 status 제거하지만 binding 만 빼고 fallback 추후 별 task
- (B) implements 의 status 유지 + X2-1 SDD line 33 갱신 (현실에 맞춤)

선택 사유를 보고 안에 명시.

### Cat 2. `accent-2-fg` / `accent-3-fg` → `.foreground` 마이그레이션 (X2-2 follow-up)

**현 상태**: 4 파일에 `accent-{2,3}-fg` 잔존
- `packages/design/tokens/semantic/core.json` (2 hits)
- `packages/design/tokens/themes/default-dark.json` (2 hits)
- `packages/design/tokens/themes/veronex-light.json` (2 hits)
- `packages/design/tokens/themes/veronex-dark.json` (2 hits)

**작업**:
1. source token 키 rename: `"accent-2-fg"` → DTCG nested `"accent-2": { "$root": {...}, "foreground": {...} }` 또는 sibling `"accent-2-foreground"` (다른 status/destructive 패턴 따름 — `accent.foreground` 가 이미 nested 면 nested 로).
2. 빌드 후 CSS variable: `--vds-theme-accent-2-fg` → `--vds-theme-accent-2-foreground` 확인.
3. `@verobee/codemods` `migrate-naming-2026-05` 코드맵 에 rename 행 추가 (consumer 가 `--vds-theme-accent-2-fg` 참조하면 변환되도록).
4. CHANGELOG 갱신.

검증:
```sh
rg "accent-[23]-fg\b" packages/design/tokens/ packages/design/dist/css/
# 결과 0 = PASS
rg -- "--vds-theme-accent-[23]-foreground" packages/design/dist/css/themes/
# 결과 양수 = PASS (4 theme × 2 vars)
```

### Cat 3. docs `vds-text-primary-fg` example 갱신

`docs/llm/build/consumer.md:35`:
```html
<button class="vds-bg-primary vds-text-primary-fg vds-py-3 vds-px-4 vds-rounded-md vds-text-sm vds-font-medium">
```

`docs/llm/llm-context/reference-cheatsheet.md:153`:
```html
<button class="vds-bg-primary vds-text-primary-fg vds-p-3 vds-rounded-md">
```

`vds-text-primary-fg` → `vds-text-primary-foreground` (또는 현 utility 가 발행하는 정확한 클래스명. utility class 산출이 `vds-text-primary-foreground` 인지 확인 후 적용).

확인:
```sh
rg "vds-text-[a-z]+-foreground" packages/utilities/dist/ | head -5
# 또는 packages/design/dist/css/utilities/
```

검증:
```sh
rg "vds-text-primary-fg|vds-theme-[a-z-]+-fg\b" docs/llm/
# 결과 0 = PASS
```

### Cat 4. `aaa-strict` policy 명시 (audit 기준 명확화)

X2-3 SDD: aaa-strict 는 **optional opt-in**. 현재 0 brand opt-in = valid.

**작업**: audit 기준 명확화 + 문서 명시.
1. `docs/llm/tokens/contrast.md` 에 "AAA-strict opt-in 0 brand = valid (optional group, opt-in 시 brand 가 `implements` 에 `aaa-strict` 추가)" 한 문장 추가.
2. `.specs/verodesign/2026-05-21-handoff/codex-prompts/x4-final-audit.txt` 의 G5 검수 기준 갱신:
   - 변경 전: report 에 AAA-strict required hit > 0
   - 변경 후: validator 가 group-aware/tier-aware (`audit/contrast.mjs` 코드 grep) + report 에 `required: "AAA-strict"` pair 개수 = brand opt-in 수 × 슬롯 (현재 0 brand opt-in = 0 pair = PASS)
3. 본 task 에서 brand opt-in 추가는 하지 않음 (별 향후 결정).

검증:
```sh
rg "aaa-strict.*opt-in.*valid" docs/llm/tokens/contrast.md
# 결과 양수 = PASS
```

## 빌드 + 통합검증

```sh
pnpm --filter @verobee/design build
pnpm --filter @verobee/design-elements build

# 빌드 PASS 후
rg "accent-[23]-fg\b" packages/design/tokens/ packages/design/dist/css/  # 0
rg "vds-text-primary-fg|vds-theme-[a-z-]+-fg\b" docs/llm/  # 0
rg "\"status\"" packages/design/tokens/themes/{verobase,verobase-admin,_template}-{light,dark}.json packages/design/tokens/themes/veronex-light.json  # 0 ("status" implements 잔존 0)
rg "\"status\"" packages/design/tokens/themes/{default-dark,veronex-dark,girok-light,girok-dark}.json  # >= 4 (보존)

# WCAG audit 통과
# 빌드 stdout 의 "WCAG contrast... ok" 확인
```

## 절대 금지

- audit 무관 영역 (X2-4 workflow, X2-5 docs, X2-6 experimental isolation) 변경
- AI/LLM/Codex/Claude 멘션 (commit/diff 어디든)
- 자가 PASS 선언 (검증은 stdout 인용)

## 출력 보고

- 4 카테고리 각각 결과
- Cat 1 의 (A)/(B) 선택 사유 (Consumer 영향 검토 후)
- 빌드 결과 (`pnpm --filter @verobee/design build` + `@verobee/design-elements build`)
- 검증 명령 stdout 인용
- 남은 위험 / follow-up 권고
