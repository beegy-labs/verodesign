# Scope — 라운드2 B3(verodesign): 순수 종료 확인 (canon §5, cross-brand zero diff)
B1=Knip 전부 false-positive(순수 no-find). B2=토큰 합치기 후보 정책위반
거부·revert(net 0, 순수 미발견 아님). B3 = **독립 순수 확인 패스**:
전 렌즈(dead-code/토큰 SSOT·중복·미사용/순수성/간결화) 재검증.
- 디자인토큰: 동일 '의도'+동일 해석값 진짜 중복만(info≠primary,
  error≠destructive 는 의도 distinct = 합치기 금지, B2 교훈). 미사용
  토큰은 cross-brand+소비자(app-girok file:) 참조 0 확증 시만, 아니면
  DEFER/.add/deprecate.md 후보.
- design-react/elements src: 데드/중복/순수성 — 신규 입증·zero
  observable·cross-brand zero diff 만.
신규 유효 발견 없으면 전영역 "순수 미발견/수렴" 확정 선언(예상).
토큰값/색/테마출력 불변, design-pkg dist/typography/build-script 미접촉.
스냅샷 /tmp/bdrift-snap/vero-pre-b3.tgz. Knip/build/typecheck 델타 악화 0.
응답≤400: 전영역 순수 미발견 확정(또는 유효 발견표) + cross-brand 근거
+ 검증 + footprint.
