# Changelog

## [Unreleased]

- feat(elements): add `vds-progress`, `vds-date-grid`, and `vds-settings-row`
- feat(elements): extend `vds-tabs` with opt-in `indicator="slide"` segmented indicator mode
- feat(elements): add `vds-icon` registry component with 35 vendored Lucide registrations
- feat(elements): add vds-binary-pill-toggle, vds-compact-row
- breaking(elements): rename consumed theme foreground/status CSS variables to DTCG-aligned forms such as `--vds-theme-primary-foreground` and `--vds-theme-status-success-foreground`
