# Changelog

## [Unreleased]

- feat(react): add `Progress`, `DateGrid`, and `SettingsRow` composite adapters
- feat(react): extend `Tabs` with `indicator="slide"` prop support
- feat(react): add 35 named icon adapters backed by `vds-icon`
- design-react: all primitives hand-written React; zero Lit runtime dependency in React package
- feat(react): add BinaryPillToggle adapter, promote CompactRow to Lit-backed
- breaking(react): rename consumed theme foreground/status CSS variables to DTCG-aligned forms such as `--vds-theme-primary-foreground` and `--vds-theme-status-success-foreground`
